This project designed two classifiers: a two-layer neural network, a perceptron classifier. 

It tests classifiers on two image data sets: a set of scanned handwritten digit images and a set of face images.
